<%# form_with url: admin_products_bulk_delete_path, method: :post, id: :bulk_delete_form  do |f| %>
    <%# f.button type: :submit, class: "btn-success" do %>
    <%# end %>
<%# end %>

  <%= form_with url: delete_selected_admin_products_path, method: :post do |f| %>
    <%= f.hidden_field :product_ids, class: 'bulk_product_ids'  %>
    <%= f.submit "Delete selected", class: 'btn btn-secondary', id: 'deleteAll' %>
  <% end %>
  <%= form_with url: edit_products_taxon_admin_products_path, method: :post do |f| %>
    <%= f.hidden_field :product_ids, class: 'bulk_product_ids'  %>
    <%= f.submit "Update taxon", class: 'btn btn-secondary', id: 'updateTaxon' %>
  <% end %>
  <%# link_to 'Update taxon', edit_products_taxon_admin_products_path, class: 'btn btn-secondary', id: 'updateTaxon' %>
